extends layout 
block content 
  h2 User List 
  if users.length 
    ul 
      each user in users 
        li
        = user.name  
        a(href = `/edit-user/${user._id}`)   Edit 
        a(href = `/delete-user/${user._id}`)   Delete 
  else 
    p No users found.